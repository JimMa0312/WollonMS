@model IEnumerable<WollonMe.Models.PerKind>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PerKindName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PerKindName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PerKindId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PerKindId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PerKindId })
        </td>
    </tr>
}

</table>
